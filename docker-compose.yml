services:
  api:
    # ... outras configurações ...
    volumes:
      # - ./:/app  <-- COMENTE ESTA LINHA ADICIONANDO UM '#'
      - ./librechat.yaml:/app/librechat.yaml
      - ./data:/app/data
      - ./.env:/app/.env
      - ${IMAGES_PATH}:/app/client/public/images
    # ... resto do arquivo ...
